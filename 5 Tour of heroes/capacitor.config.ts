import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'angular.tour.of.heroes',
  appName: 'angular-tour-of-heroes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
